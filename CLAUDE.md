# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

```bash
npm install       # Install dependencies and rebuild native modules for Electron
npm start         # Start dev server with hot reload (webpack watches src/, outputs to app/)
npm run unit      # Run unit tests (webpack compiles src/**/*.spec.js → electron-mocha)
npm test          # Run unit tests (same as npm run unit)
npm run release   # Test → webpack build → package with electron-builder to dist/
```

There is no linter configured.

To run a single unit test, add a `.only` modifier to the `describe` or `it` block in the spec file, then run `npm run unit`.

## Architecture

This is an **Electron desktop application** for a Bank of America (BOA) "Soccer at Schools" baseball event scoreboard. It embeds an **Express + Socket.io server** (port 5000) that drives real-time score display across multiple connected clients.

### Process Model

- **`src/background.js`** — Electron main process. Creates BrowserWindows, manages auto-updates via electron-updater (publishes to S3 bucket `app-bmw-scoreboard`), and handles IPC. Uses `@electron/remote/main` — must call `remoteMain.initialize()` on ready and `remoteMain.enable(win.webContents)` for each window.
- **`src/app.js`** — Electron renderer process. Hosts the main UI, the Express/Socket.io server, and all app processing. Keep all business logic here — do not move to background.js.
- **`src/express.js`** — Express server that runs inside the Electron renderer process. Serves EJS views from `app/expressviews/`, static assets from `app/expressassets/`, and exposes the Socket.io server for real-time communication.

### Key Source Files

| File | Purpose |
|---|---|
| `src/scoreboard.js` | Scoreboard display logic, animations, leaderboard cycling |
| `src/registration.js` | Game participant registration form |
| `src/admin.js` | Admin interface |
| `src/settings.js` | Settings panel (syncs with photo booth, screensaver, fonts, timing, takeover schedule) |
| `src/update.js` | Auto-update handler |
| `src/shareapp.js` | Integration with external share app |

### Build System

- **Source:** `src/` (ES6, transpiled via Babel targeting Electron 33)
- **Output:** `app/` (compiled JS, HTML views, CSS, assets)
- **Bundler:** Webpack 5 with configs in `build/`
  - `webpack.app.config.js` — all renderer/main process entry points
  - `webpack.unit.config.js` — dynamically discovers `*.spec.js` files
  - `webpack.e2e.config.js` — dynamically discovers `*.e2e.js` files

Never edit files in `app/` directly — they are generated by webpack from `src/`.

**Exceptions — static files edited directly (not webpack-generated):**
- `app/scoreboard.html` — scoreboard window layout and styles
- `app/app.html` — main control panel UI
- `app/expressviews/registration.ejs` — player registration page (Express/EJS, not webpack)
- `app/expressviews/*.ejs` — all Express views

### Data & Storage

- **Database:** NeDB embedded NoSQL at `C:/SB-Scoreboard/scoreboard.db`
- **Settings:** electron-json-storage for persistent app settings
- **Logs:** electron-log writing to `C:/SB-Scoreboard/logs/app.log`
- **Stock players:** Loaded from `C:/SB-Scoreboard/stockPlayers.json`

### Branding & Colors

Brand palette (BOA / Soccer at Schools):
- **Navy:** `#002868`
- **Red:** `#c41230`
- **White:** `#ffffff`

Registration page (`app/expressviews/registration.ejs`) uses this palette: navy background, white inputs, red submit button.

### Small Business Take Over (SBTO) Overlay

Images are loaded from `C:/SB-Scoreboard/sbto/` (scanned at startup). The app cycles through all images in alphabetical order before repeating — each trigger shows the next image. Falls back to `banking.png` if the folder is missing or empty.

Settings fields (stored under the `settings` key in electron-json-storage):
- `sbTakeoverTimes` — comma-separated minutes, e.g. `15, 45` — fires at that minute of **every** hour
- `sbTakeoverDuration` — seconds to display the overlay (default 30)

IPC flow: `src/app.js` `sbTakeoverStart()` picks the next image → sends `sbTakeoverStart` with `{ duration, image }` → `src/background.js` relays to `scoreboardWindow` → `src/scoreboard.js` updates `#sbTakeover img` src, fades in (z-index 10002), waits, fades out, resumes leaderboard cycle from `currentPage`.

New scores arriving during a takeover are queued in `newLeaderboard` and processed after the overlay fades out (`sbTakeoverActive` flag gates `checkScores`).

Settings apply immediately on save — `settings.js` sends `settingsUpdated` IPC → `background.js` relays to both `mainWindow` and `scoreboardWindow` → each updates its local variables without a reload.

`app.html` shows the current SBTO schedule and duration, and has a "Trigger Now" button (debounced 3 s, leading edge via `_.debounce`) that fires `sbTakeoverStart` manually.

### Bus Take Over

Triggers relays (via `CommandApp_USBRelay.exe` in `extraResources/`), plays `C:/SB-Scoreboard/busstakeover.mp3`, fires the DMX cannon, and turns on the DMX fans.

Settings fields:
- `busTakeoverTimes` — comma-separated minutes (used when mode is `timer`)
- `busTakeoverMode` — `'timer'` | `'highscore'`
- `busHighScoreCooldown` — seconds between high-score triggers (default 60)
- `busRelaySerialNumber` — USB relay device serial
- `busRelay1Delay` / `busRelay1OnTime` — ms delay before firing / ms relay stays on (channel 01)
- `busRelay2Delay` / `busRelay2OnTime` — same for channel 02
- `busMp3Delay` — ms after trigger before audio plays
- `busAudioVolume` — 0–1 float
- `busFadeOutDuration` — ms to fade audio out when relay 1 turns off; `0` = instant cut (default 2000)
- `busCannonDelay` — ms after trigger before firing DMX cannon (default 0)
- `busFanDelay` — ms after trigger before turning on DMX fans (default 0)
- `busFanOnTime` — ms fans stay on before auto-off (default 5000)

**Top-3 trigger IPC flow:**
`src/scoreboard.js` `showScore()` (when displayed score is in leaderboard top 3)
→ `ipcRenderer.send('busHighScoreTrigger')`
→ `src/background.js` relays to `mainWindow`
→ `src/app.js` checks mode + cooldown → `runBusTakeover()`

Audio fades out over `busFadeOutDuration` ms when relay 1 turns off (triggered by `fadeOutBusAudio()`).

### DMX Controls (Cannon + Fans)

Two DMX fixtures are controlled via a single **Enttec USB DMX Pro** adapter. Uses `serialport` 12.x (`@serialport/bindings-cpp` — NAPI-based, Electron-version-agnostic). The `dmx` npm package was explicitly **not** used because it depends on `@serialport/bindings@8.x` which has no prebuilt binary for Electron 33 and fails to compile on this machine.

The Enttec USB DMX Pro framing protocol is implemented directly in `src/app.js` (`sendDmxPacket`):
- Open serial port at 57600 baud, 8N1
- Packet format: `0x7E 0x06 [dataLen LSB] [dataLen MSB] 0x00 [512 channel bytes] 0xE7`

**Cannon** settings fields:
- `dmxPort` — serial port for the adapter (e.g. `COM3`); blank = DMX disabled
- `dmxChannel` — DMX channel number (1–512)
- `dmxCannonOnValue` — DMX value (0–255) sent on Cannon On (default 255)
- `dmxCannonOffValue` — DMX value (0–255) sent on Cannon Off (default 0)
- `dmxCannonAutoOff` — ms after Cannon On before automatically sending Cannon Off; `0` = disabled

**Cannon test mode** cycles: On → `autoOff` ms (min 1000) → Off → 5 s → repeat, logging a fire count (`#N`) each cycle. Counter resets on each start. Implemented via `runCannonTestCycle()` / `startCannonTest()` / `stopCannonTest()`.

**Fans** settings fields:
- `dmxFanChannel` — DMX channel number (1–512)
- `dmxFanOnValue` — DMX value (0–255) sent on Fans On (default 255)
- `dmxFanOffValue` — DMX value (0–255) sent on Fans Off (default 0)

`app.html` has a **DMX Cannon** card (status, On/Off, Test Mode toggle) and a **DMX Fans** card (On/Off). Both share the same serial port; the port is re-opened whenever settings are saved.

### Scoreboard Display (`app/scoreboard.html` + `src/scoreboard.js`)

- Leaderboard renders up to 24 entries in a single table (`#leaderboard1`), 4 rows per page, cycling every `cycleInterval` ms when > 4 entries.
- Each row is **3 columns**: `.place` (rank) | `.initial` (full name) | `.score`.
- Names are stored as `"FirstName L."` (full first name + last initial), displayed uppercase.
- Registration form (`app/expressviews/registration.ejs`) captures `firstName` (free text) and `lastName` (1 character max); `src/app.js` composes `nickname` before DB insert.
- **New score overlay** (`#newScore`) shows `"NAME's SCORE"` in `#newScoreName` and the score in `#newScoreText`.
- **New high score overlay** (`#newHighScore`) — shown instead of `#newScore` when the player takes the #1 spot. Uses `newHighScore.png` background. Shows only the player name (no `'s SCORE`). Same child elements pattern: `#newHighScoreName` / `#newHighScoreText`.
- Font: `Connections` (loaded from `app/fonts/`).
- `cycleTimer` lifecycle: always clear before setting; `showScore` guards start with `!sbTakeoverActive`; SBTO complete clears before restart.

### Socket.io Events (port 5000)

| Event | Direction | Purpose |
|---|---|---|
| `startGame` | client → server | Begin game with player info |
| `gameEnd` | server → clients | Notify game ended |
| `updateScore` | server → clients | Broadcast updated score + leaderboard |
| `zerobat` | client → server | Request bat zero |
| `zeroprompt` | server → clients | Display zero bat prompt |

### Environment Config

Environment-specific settings live in `config/env_development.json`, `config/env_production.json`, and `config/env_test.json`. The build process selects the appropriate config.
